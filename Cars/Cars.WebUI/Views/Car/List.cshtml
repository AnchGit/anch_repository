@using Cars.Domain.Entities
@model IEnumerable<Car>

@{
    ViewBag.Title = "Cars";
}

@foreach (var car in Model)
{
    <div>
        <h3>@car.Model</h3>
        <h4>@car.Price.ToString("c")</h4>
    </div>
}

